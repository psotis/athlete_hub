import 'package:athlete_hub/helpers/imports.dart';

class CalendarMobile extends StatelessWidget {
  const CalendarMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: CalendarAppbar());
  }
}

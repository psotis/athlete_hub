import 'package:flutter/material.dart';

class MetricsDesktop extends StatelessWidget {
  const MetricsDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

import 'package:flutter/material.dart';

class CalendarDesktop extends StatelessWidget {
  const CalendarDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
